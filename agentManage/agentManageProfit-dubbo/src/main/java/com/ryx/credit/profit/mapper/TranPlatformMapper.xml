<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ryx.credit.profit.dao.TranPlatformMapper">
  <resultMap id="BaseResultMap" type="com.ryx.credit.profit.pojo.TranPlatform">
    <id column="ID" jdbcType="VARCHAR" property="id" />
    <result column="PLATFORM_NAME" jdbcType="VARCHAR" property="platformName" />
    <result column="PLATFORM_TYPE" jdbcType="VARCHAR" property="platformType" />
  </resultMap>
  <sql id="Example_Where_Clause">
    <where>
      <foreach collection="oredCriteria" item="criteria" separator="or">
        <if test="criteria.valid">
          <trim prefix="(" prefixOverrides="and" suffix=")">
            <foreach collection="criteria.criteria" item="criterion">
              <choose>
                <when test="criterion.noValue">
                  and ${criterion.condition}
                </when>
                <when test="criterion.singleValue">
                  and ${criterion.condition} #{criterion.value}
                </when>
                <when test="criterion.betweenValue">
                  and ${criterion.condition} #{criterion.value} and #{criterion.secondValue}
                </when>
                <when test="criterion.listValue">
                  and ${criterion.condition}
                  <foreach close=")" collection="criterion.value" item="listItem" open="(" separator=",">
                    #{listItem}
                  </foreach>
                </when>
              </choose>
            </foreach>
          </trim>
        </if>
      </foreach>
    </where>
  </sql>
  <sql id="Base_Column_List">
    ID, PLATFORM_NAME, PLATFORM_TYPE
  </sql>
  <select id="selectByExample" parameterType="com.ryx.credit.profit.pojo.TranPlatformExample" resultMap="BaseResultMap">
    <include refid="OracleDialectPrefix" />
    select
    <if test="distinct">
      distinct
    </if>
    <include refid="Base_Column_List" />
    from TRAN_PLATFORM
    <if test="_parameter != null">
      <include refid="Example_Where_Clause" />
    </if>
    <if test="orderByClause != null">
      order by ${orderByClause}
    </if>
    <include refid="OracleDialectSuffix" />
  </select>
  <select id="selectByPrimaryKey" parameterType="java.lang.String" resultMap="BaseResultMap">
    select 
    <include refid="Base_Column_List" />
    from TRAN_PLATFORM
    where ID = #{id,jdbcType=VARCHAR}
  </select>
  <delete id="deleteByExample" parameterType="com.ryx.credit.profit.pojo.TranPlatformExample">
    delete from TRAN_PLATFORM
    <if test="_parameter != null">
      <include refid="Example_Where_Clause" />
    </if>
  </delete>
  <insert id="insert" parameterType="com.ryx.credit.profit.pojo.TranPlatform">
    insert into TRAN_PLATFORM (ID, PLATFORM_NAME, PLATFORM_TYPE
      )
    values (#{id,jdbcType=VARCHAR}, #{platformName,jdbcType=VARCHAR}, #{platformType,jdbcType=VARCHAR}
      )
  </insert>
  <insert id="insertSelective" parameterType="com.ryx.credit.profit.pojo.TranPlatform">
    insert into TRAN_PLATFORM
    <trim prefix="(" suffix=")" suffixOverrides=",">
      <if test="id != null">
        ID,
      </if>
      <if test="platformName != null">
        PLATFORM_NAME,
      </if>
      <if test="platformType != null">
        PLATFORM_TYPE,
      </if>
    </trim>
    <trim prefix="values (" suffix=")" suffixOverrides=",">
      <if test="id != null">
        #{id,jdbcType=VARCHAR},
      </if>
      <if test="platformName != null">
        #{platformName,jdbcType=VARCHAR},
      </if>
      <if test="platformType != null">
        #{platformType,jdbcType=VARCHAR},
      </if>
    </trim>
  </insert>
  <select id="countByExample" parameterType="com.ryx.credit.profit.pojo.TranPlatformExample" resultType="java.lang.Long">
    select count(*) from TRAN_PLATFORM
    <if test="_parameter != null">
      <include refid="Example_Where_Clause" />
    </if>
  </select>
  <update id="updateByPrimaryKeySelective" parameterType="com.ryx.credit.profit.pojo.TranPlatform">
    update TRAN_PLATFORM
    <set>
      <if test="platformName != null">
        PLATFORM_NAME = #{platformName,jdbcType=VARCHAR},
      </if>
      <if test="platformType != null">
        PLATFORM_TYPE = #{platformType,jdbcType=VARCHAR},
      </if>
    </set>
    where ID = #{id,jdbcType=VARCHAR}
  </update>
  <update id="updateByPrimaryKey" parameterType="com.ryx.credit.profit.pojo.TranPlatform">
    update TRAN_PLATFORM
    set PLATFORM_NAME = #{platformName,jdbcType=VARCHAR},
      PLATFORM_TYPE = #{platformType,jdbcType=VARCHAR}
    where ID = #{id,jdbcType=VARCHAR}
  </update>
  <sql id="OracleDialectPrefix">
    <if test="page != null">
      select * from ( select row_.*, rownum rownum_ from ( 
    </if>
  </sql>
  <sql id="OracleDialectSuffix">
    <if test="page != null">
      <![CDATA[ ) row_ ) where rownum_ > #{page.begin} and rownum_ <= #{page.end} ]]>
    </if>
  </sql>
  <!-- ======================以上为自动生成代码，请务必把自己添加代码写到下面，防止被覆盖================================-->
  <select id="selectAllTranPlatform" resultType="java.util.Map">
    select PLATFORM_NAME,PLATFORM_TYPE from TRAN_PLATFORM order by PLATFORM_TYPE
  </select>
  <select id="selectAllPlatformType" resultType="java.lang.String">
    select distinct PLATFORM_TYPE from TRAN_PLATFORM order by PLATFORM_TYPE
  </select>
  <select id="getTranAmtByMonth" resultType="java.util.Map">
      select
            (select sum(IN_TRANS_AMT)from TRANS_PROFIT_DETAIL
                where PROFIT_DATE=#{tranMonth}
                and SOURCE_INFO='MPOS')+(select  sum(TRANS_AMT)from P_PROFIT_DIRECT where TRANS_MONTH=#{tranMonth})
                as MPOS_TRAN_AMT,
             (select sum(TRANS_FEE)from TRANS_PROFIT_DETAIL
                where PROFIT_DATE=#{tranMonth}
                and SOURCE_INFO='MPOS')+(select  sum(TRANS_FEE)from P_PROFIT_DIRECT where TRANS_MONTH=#{tranMonth})
                as TRANS_FEE
       from dual
  </select>
</mapper>